# Results Directory

This directory stores machine-readable experiment outputs, summary tables, and paper-ready figures for the SS-CBT synthetic benchmark.

The default run created by `run.sh` writes into `results/default_run/`.
The MT-Bench real-data illustration writes into `results/real_run/`.
